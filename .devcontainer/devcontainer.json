{
    "name": "Existing Docker Compose (Extend)",
    "dockerComposeFile": [
        "compose.yaml"
    ],
    "service": "php-app",
    "workspaceFolder": "/var/www/html",
    "settings": {},
    "runArgs": [
        "--env-file", "../.docker/local/php/.env.app"
    ]
}
